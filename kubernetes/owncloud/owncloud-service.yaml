apiVersion: v1
kind: Service
metadata:
  name: owncloud-service
  namespace: owncloud
spec:
  selector:
    app: owncloud
  ports:
  - port: 80
    targetPort: 8080
    nodePort: 30090  
    protocol: TCP
  type: LoadBalancer 