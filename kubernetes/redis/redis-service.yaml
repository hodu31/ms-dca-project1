apiVersion: v1
kind: Service
metadata:
  name: redis-service
  namespace: owncloud
spec:
  selector:
    app: redis
  ports:
  - port: 6379
    targetPort: 6379
    protocol: TCP
  clusterIP: None  # Headless service for StatefulSet